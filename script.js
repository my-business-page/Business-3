document.addEventListener('DOMContentLoaded',()=>{const loader=document.querySelector('.loader');if(loader){setTimeout(()=>loader.classList.add('loaded'),700)}const header=document.querySelector('.site-header');const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');if(toggle&&nav){toggle.addEventListener('click',()=>{nav.classList.toggle('open');toggle.textContent=nav.classList.contains('open')?'CLOSE':'MENU'})}window.addEventListener('scroll',()=>{if(header)header.classList.toggle('scrolled',window.scrollY>40)});const items=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible');else e.target.classList.remove('visible')}),{threshold:.12});items.forEach(item=>observer.observe(item));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const canvas=document.getElementById('vfx');if(canvas){const ctx=canvas.getContext('2d');let w=0,h=0;const dots=Array.from({length:45},()=>({x:Math.random(),y:Math.random(),r:.5+Math.random()*1.6,v:.00015+Math.random()*.00035}));const resize=()=>{w=canvas.width=innerWidth;h=canvas.height=innerHeight};resize();addEventListener('resize',resize);const draw=()=>{ctx.clearRect(0,0,w,h);dots.forEach(d=>{d.y-=d.v;if(d.y<0)d.y=1;ctx.beginPath();ctx.arc(d.x*w,d.y*h,d.r,0,Math.PI*2);ctx.globalAlpha=.25;ctx.fillStyle='rgba(255,245,220,.8)';ctx.fill()});requestAnimationFrame(draw)};draw()}});